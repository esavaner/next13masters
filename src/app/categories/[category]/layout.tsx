type Props = {
  children?: React.ReactNode;
};

// export const generateStaticParams = async () => {
//   return [{ category: 't-shirts' }, { category: 'boots' }];
// };

export default function CategoryPageLayout({ children }: Props) {
  return children;
}
